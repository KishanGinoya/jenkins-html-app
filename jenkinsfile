pipeline {
    agent any

    stages {

        stage('Show Files') {
            steps {
                sh 'ls'
            }
        }

        stage('Read HTML') {
            steps {
                sh 'cat index.html'
            }
        }

    }
}
